# Core dependencies
flask>=3.0
gunicorn>=22.0
numpy>=2.0
pandas>=2.2
scipy>=1.14
scikit-learn>=1.5
requests>=2.32
pyarrow>=17.0

# Miner dependencies
boto3>=1.35
bittensor>=7.0
cryptography>=42.0
# timelock — installed from local source (../timelock-src/), not on PyPI

# Targon server dependencies (remote mode)
fastapi>=0.115
uvicorn>=0.32
